<h2>Edit scheduler</h2>

<%= render "form.html", changeset: @changeset,
                        action: scheduler_path(@conn, :update, @scheduler) %>

<%= link "Back", to: scheduler_path(@conn, :index) %>
